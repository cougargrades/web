
# @cougargrades/models

A replacement for [@cougargrades/types](https://github.com/cougargrades/types). Houses CougarGrade's own models. All models defined with Zod schemas so they can be validated at runtime. Often will also have "helper" functions that are model-specific.
