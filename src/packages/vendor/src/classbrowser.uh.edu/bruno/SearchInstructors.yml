info:
  name: SearchInstructors
  type: http
  seq: 21

http:
  method: GET
  url: "{{BASE}}/api/instructor?instructor=Buzzanco"
  params:
    - name: instructor
      value: Buzzanco
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
