info:
  name: GetAllSubjects
  type: http
  seq: 1

http:
  method: GET
  url: "{{BASE}}/api/subjects"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
