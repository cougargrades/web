info:
  name: GetSubjectsForTerm
  type: http
  seq: 6

http:
  method: GET
  url: "{{BASE}}/api/online/term/subjects?term=2280"
  params:
    - name: term
      value: "2280"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
